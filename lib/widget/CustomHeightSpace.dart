import 'package:flutter/material.dart';

class CustomHeightSpace extends StatelessWidget {
  const CustomHeightSpace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 16,
    );
  }
}
